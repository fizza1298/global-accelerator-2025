name: Keep Render Alive

on:
  schedule:
    - cron: "*/60 * * * *" # Every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: curl -s https://skillbridge-d7z9.onrender.com > /dev/null
